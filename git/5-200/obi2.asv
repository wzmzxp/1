function [ output_args ] = obi2( cita )
%OBI2 此处显示有关此函数的摘要
%   此处显示详细说明
% global s citaf citar citam;
delta=0;j=0;ganma=0;
ksi=arctan(cos(ganma)*tan(cita));
a2=(4140+delta*tan(13))*(1-exp(-100*j))*cos(ksi)-de;ta*sin(ksi);
a3=0.055*power(cos(cita)*cos(cita)+sin(cita)*sin(cita)*sec(ganma));
output_args=a2*a3;
end

