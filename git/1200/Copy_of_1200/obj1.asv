function [outputArg1,outputArg2] = obj1(h1,up1)
global N h up
h=h1;up=up1;
for k=1:N
    a1=(k-1)*Ck+Cp;
    Ppk1=Ppk(k*h+up,k*h);
    a2=(k-1)*Ck+Cf;
    Pfk1=Pfk(k*h,(k-1)*h);
    fenzi=a1*Ppk1+a2*Pfk;
    a3=E()
    a3=k*h*Ppk1+
end
end

