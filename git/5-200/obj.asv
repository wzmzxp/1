function [ output_args ] = obj( ganma )
global s citaf citar citam;
delta=0;j=0;ksi=0;
a1=0.16*ganma;
% a2=(4140+delta*tan(13))*(1-exp(-100*j))*cos(ksi)-de;ta*sin(ksi);
% a3=0.055*power(cos())
a1*quad(@obi2,citar,citaf);
end

