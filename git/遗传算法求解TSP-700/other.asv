function [ output_args ] = other( input_args,D )

%各地之间的红灯数目
reds=randi(200,31,31);
for i=1:31
    for j=1:31
        reds(j,i)=reds(i,j);
    end
end
%各地之间的路况系数
roads=rand(31,31);
for i=1:31
    for j=1:31
        roads(j,i)=roads(i,j);
    end
end
%各地之间的堵车系数
crowds=rand(31,31);
for i=1:31
    for j=1:31
        crowds(j,i)=crowds(i,j);
    end
end

end

